#ifndef TapConstraintType_h
#define TapConstraintType_h

#include "application.h"

class TapConstraintType {
 public:
  enum e { NONE = 0, PURCHASE_VOLUME = 1, MAXIMUM_VOLUME = 2 };
};

#endif
